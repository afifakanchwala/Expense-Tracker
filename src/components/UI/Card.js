// import React from "react";
// import "./Card.css";

// const Card = (props) => {
//   const classes = "card" + props.className;
//   //props.children is a special prop in react which will yield the content passed btw
//   //the opening tag and closin tag
//   return <div className={classes}>{props.children}</div>;
// };

// export default Card;
